UNIFIED LOAD BOARD — EVERY MORNING (2 minutes)
==============================================

1. Double-click:  Desktop\unified-load-board\DAILY_START.bat
2. In the scanner Chrome window that opens, sign into:
     - Arrive
     - RXO Connect
     - ArcBest
     - EchoDrive  → Available Loads → Romeoville 100mi → set pickup dates → Search
3. Leave THAT Chrome open all day (minimize OK, do not close)
4. Open board: http://localhost:8765/  (Ctrl+F5 once)

If AI Load Offers pings "board stale" — run DAILY_START.bat again and re-check logins.

Do NOT close the two minimized Python windows (table + scanner).
